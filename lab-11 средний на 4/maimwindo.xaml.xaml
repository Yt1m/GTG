<Window x:Class="BinaryFileConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Binary File Converter" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="InputTextBox" Grid.Row="0" Margin="10" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button x:Name="WriteToFileButton" Content="Записать в файл" Margin="5" Click="WriteToFileButton_Click"/>
            <Button x:Name="ConvertButton" Content="Преобразовать" Margin="5" Click="ConvertButton_Click"/>
        </StackPanel>
        <TextBlock x:Name="OutputTextBlock" Grid.Row="2" Margin="10"/>
    </Grid>
</Window>

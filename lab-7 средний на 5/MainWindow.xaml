<Window x:Class="QueueImplementation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Реализация очереди" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBox x:Name="PhoneTextBox" Width="150"/>
            <TextBox x:Name="PriceTextBox" Width="100"/>
            <Button Content="Добавить" Click="EnqueueButton_Click" Margin="10,0,0,0"/>
        </StackPanel>
        <Button Content="Удалить" Click="DequeueButton_Click" Margin="365,10,275,7"/>
        <TextBox x:Name="QueueDisplayTextBox" Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
